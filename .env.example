# Discord × Copilot Agent — Environment Variables
# Copy this file to .env and fill in your values.

# Required: Discord bot token
# Get one at: https://discord.com/developers/applications
DISCORD_TOKEN=

# Required: Git repository URL (HTTPS or SSH)
REPO_URL=

# Optional: GitHub personal access token (for private repos / higher rate limits)
# Create at: https://github.com/settings/tokens  — needed scope: repo
GITHUB_TOKEN=

# Optional: Restrict bot to specific guilds (comma-separated IDs)
# ALLOWED_GUILDS=

# Optional: Restrict bot to specific channels (comma-separated IDs)
# ALLOWED_CHANNELS=

# Optional: Restrict commands to users with these roles (comma-separated IDs)
# ADMIN_ROLE_IDS=
